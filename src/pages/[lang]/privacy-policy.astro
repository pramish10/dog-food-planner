---
import LocalizedLegalPage from '../../components/LocalizedLegalPage.astro';
import { getLangStaticPaths } from '../../i18n/utils';
import type { Lang } from '../../i18n/ui';
export function getStaticPaths() { return getLangStaticPaths(); }
const { lang } = Astro.params as { lang: Lang };
---
<LocalizedLegalPage lang={lang} kind="privacy" titleKey="privacy.title" navKey="footer.linkPrivacy" />
